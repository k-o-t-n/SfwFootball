@model Sfw.Football.Models.StandingsModel
@{
    ViewBag.Title = "Home Page";
}

<div class="col-md-4 col-md-offset-4">
    <table class="table" id="standings">
        <thead>
            <tr>
                <th>Position</th>
                <th>Player</th>
                <th>Points</th>
                <th>PPG</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.AllPlayers.Count(); i++)
            {
                <tr>
                    <td>@(i + 1)</td>
                    <td>@Html.DisplayFor(m => m.AllPlayers.ElementAt(i).Name)</td>
                    <td>@Html.DisplayFor(m => m.AllPlayers.ElementAt(i).Points)</td>
                    <td>@Html.DisplayFor(m => m.AllPlayers.ElementAt(i).PointsPerGame)</td>
                </tr>
            }
        </tbody>
    </table>
</div>